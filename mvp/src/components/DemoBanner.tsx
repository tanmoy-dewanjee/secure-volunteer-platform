// Shows that this website is only a project demo.

export default function DemoBanner() {
    return (
        <div
            role="status"
            className="bg-[#f4d35e] px-4 py-2 text-center text-sm font-semibold text-black"
        >
            DEMO WEBSITE — Educational Prototype. Not an official Adelaide University
            production service.
        </div>
    );
}